# Active Execution Plans

This directory stores execution plans for non-trivial work that is currently underway.

The directory may contain only this README between tasks. Plan validators must ignore this README and treat only task-specific Markdown files as active execution ledgers.
