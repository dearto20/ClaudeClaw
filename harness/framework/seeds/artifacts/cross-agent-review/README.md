# Cross-Agent Review Artifacts Seed

Copy this README to `harness/artifacts/cross-agent-review/README.md` only when that live target directory is missing or lacks a README anchor.

Do not overwrite existing review packets or reports. Generated evidence stays in `harness/artifacts/cross-agent-review/`.
