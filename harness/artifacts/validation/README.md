# Validation Artifacts

This directory is the generated-output location for `node harness/scripts/validate-all.mjs`.

Timestamped reports and `latest-*` files are ignored by git. Promote validation evidence by citing the generated artifact path in an execution plan rather than committing routine generated reports.
