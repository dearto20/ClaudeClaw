{
  "name": "deploy",
  "description": "Package and deploy the app to the device. Call this LAST after all other tools.",
  "input_schema": {
    "type": "object",
    "properties": {
      "name": { "type": "string", "description": "App name" }
    },
    "required": ["name"]
  }
}
