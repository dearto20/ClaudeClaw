{
  "name": "compose_display",
  "description": "Add read-only display components — summaries, stats, overviews. Use for auto-generated content that aggregates other data.",
  "input_schema": {
    "type": "object",
    "properties": {
      "page": { "type": "string", "description": "Which page/tab to add to" },
      "title": { "type": "string", "description": "Section title" },
      "key": { "type": "string", "description": "Storage key" }
    },
    "required": ["page", "key"]
  }
}
