{
  "name": "compose_tracker",
  "description": "Add tracking components — counters with progress bars, toggle checklists. Use for anything the user tracks or checks off.",
  "input_schema": {
    "type": "object",
    "properties": {
      "page": { "type": "string", "description": "Which page/tab to add to" },
      "title": { "type": "string", "description": "Section title" },
      "key": { "type": "string", "description": "Storage key" },
      "variant": { "type": "string", "enum": ["counter", "toggles"], "description": "counter = incrementing number with progress, toggles = checklist" },
      "config": { "type": "object", "description": "counter: {max, unit}. toggles: {items: [string array]}" }
    },
    "required": ["page", "variant", "key"]
  }
}
