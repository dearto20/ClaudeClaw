# Validation Artifacts Seed

Copy this README to `harness/artifacts/validation/README.md` only when that live target directory is missing or lacks a README anchor.

Do not overwrite existing validation reports. Generated evidence stays in `harness/artifacts/validation/`.
