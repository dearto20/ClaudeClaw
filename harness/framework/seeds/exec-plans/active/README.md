# Active Execution Plans Seed

Copy this README to `harness/exec-plans/active/README.md` only when that live target directory is missing or lacks a README anchor.

Do not overwrite existing active execution plans. Target-owned live state stays in `harness/exec-plans/active/`.
